import React from 'react';
import './Spinner.css'; // Make sure to create a CSS file for styling

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
